# Copilot Instructions (Read First)

You are implementing a Decision Support System (Karar Destek Sistemi) for:
"BüroTime 6 Aylık Satış Tahminine Dayalı Üretim Planlama"

Rules:
1) UI labels + MySQL table/column names MUST be Turkish.
2) Keep forecasting explainable (moving average / trend + optional seasonality).
3) Build Node.js (Express) backend + MySQL integration.
4) Build Bootstrap frontend (static HTML).
5) Provide seed SQL data (at least 24 months).
6) Provide clear README steps to run locally.

Before coding:
- propose the exact DB schema in SQL
- propose API endpoints and response formats
- confirm folder structure
Then implement incrementally.

Deliver:
- backend runnable on localhost:3000
- frontend static pages that call backend API
- SQL scripts for schema + seed
